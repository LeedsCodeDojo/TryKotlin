package dojo.leeds.problem2

import org.hamcrest.core.IsEqual.equalTo
import org.junit.Assert.assertThat
import org.junit.Test

class FizzBuzzTest {

/*    @Test
    fun `given a number which is divisble by 3 then should return "Fizz"`(){
        assertThat(fizzBuzzValue(3), equalTo("Fizz"))
        assertThat(fizzBuzzValue(9), equalTo("Fizz"))
        assertThat(fizzBuzzValue(12), equalTo("Fizz"))
        assertThat(fizzBuzzValue(18), equalTo("Fizz"))
    }

    @Test
    fun `given a number which is divisble by 5 then should return "Buzz"`(){
        assertThat(fizzBuzzValue(5), equalTo("Buzz"))
        assertThat(fizzBuzzValue(10), equalTo("Buzz"))
    }

    @Test
    fun `given a number which is divisble by 3 & 5 then should return "Fizz Buzz"`(){
        assertThat(fizzBuzzValue(15), equalTo("Fizz Buzz"))
        assertThat(fizzBuzzValue(30), equalTo("Fizz Buzz"))
        assertThat(fizzBuzzValue(45), equalTo("Fizz Buzz"))
    }

    @Test
    fun `given a number which is not divisble by 3 or 5 then should return the number`(){
        assertThat(fizzBuzzValue(1), equalTo("1"))
        assertThat(fizzBuzzValue(2), equalTo("2"))
        assertThat(fizzBuzzValue(4), equalTo("4"))
        assertThat(fizzBuzzValue(7), equalTo("7"))
        assertThat(fizzBuzzValue(8), equalTo("8"))
        assertThat(fizzBuzzValue(13), equalTo("13"))
    }

    @Test
    fun `given a number then fizzBuzz should return a list of fizzBuzzValues`(){
        val expectedValues = listOf("1","2","Fizz","4","Buzz","Fizz", "7", "8",
                "Fizz", "Buzz", "11", "Fizz", "13", "14", "Fizz Buzz", "16", "17", "Fizz", "19", "Buzz")
        assertThat(fizzBuzz(20), equalTo(expectedValues))
    } */
}